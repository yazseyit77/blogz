<%- include("partials/header") -%>

<h1>Blogs</h1>

<% blogs.forEach(blog => { %>
<div>
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>" alt="">
    <span><%= blog.created %></span>
    <p><%= blog.body %></p>
</div>
<% }) %>

<%- include("partials/footer") -%>